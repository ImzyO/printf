#include "main.h"

/**
 * _printf - produces output according to format
 * @format: character string
 *
 * Return: number of characters printed
 */

int _printf(const char *format, ...)
{
	printf("That is a very large screen\n", %s);

	while ("%s" != '\0')
	{
		printf("strlen([%s])\n");
	}
	return (strlen);
}
